## Libelle

Represents a label associated with an article within the Altazion search engine export service context.

Public properties:
- kwrd_libelle: a string containing the label of the keyword.

### TypeScript class
```typescript
interface Libelle {
  kwrd_libelle: string;
}
```